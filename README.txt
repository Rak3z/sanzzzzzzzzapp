
cambiamos bastante nuestro modelo, en pos de conseguir una mejor implementacion de la app.

En la tabla historial, el rol del usuario no se dejo comom fk, esto es debido a que al ser un tema de registro no seria deseable que se borraran si el usuario se llega a borrar
lo mismo con productos, de igual forma se considero segun el punto 4.4.1.2 de la tarea, pero entendimos registros a los productos en venta y comentarios.
